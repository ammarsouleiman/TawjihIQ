{
  "name": "tawjih-iq",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev:frontend": "pnpm --filter frontend dev",
    "dev:backend": "pnpm --filter tawjih-iq-backend dev",
    "build:frontend": "pnpm --filter frontend build",
    "build:backend": "pnpm --filter tawjih-iq-backend build"
  }
}
